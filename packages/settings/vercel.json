{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter shared build && pnpm --filter settings build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping default install, using buildCommand instead'",
  "framework": null,
  "ignoreCommand": "bash -c 'cd ../.. && git diff --quiet HEAD^ HEAD -- packages/settings packages/shared'",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, POST, PUT, DELETE, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "X-Requested-With, Content-Type, Accept"
        }
      ]
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}

