{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter shared build && pnpm --filter shell build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping default install, using buildCommand instead'",
  "framework": null,
  "ignoreCommand": "bash -c 'cd ../.. && git diff --quiet HEAD^ HEAD -- packages/shell packages/shared packages/dashboard packages/products packages/settings'",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}

