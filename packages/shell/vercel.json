{
  "buildCommand": "pnpm install && pnpm --filter shared build && pnpm --filter shell build",
  "outputDirectory": "packages/shell/dist",
  "installCommand": "echo 'Skipping default install, using buildCommand instead'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

